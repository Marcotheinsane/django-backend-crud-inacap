{% extends 'base.html' %}
{% block title %}Eliminar Genero{% endblock %}
{% block content %}

<div class="alert alert-danger shadow">
    <h3>Confirmar eliminación</h3>
    <p>¿Estás seguro de que deseas eliminar el usuario <strong>{{ sucursal.nombre }}</strong>?</p>
    <form method="post">
        {% csrf_token %}
        <button type="submit" class="btn btn-danger">Eliminar</button>
        <a href="{% url 'listar_sucursales' %}" class="btn btn-secondary">Cancelar</a>
    </form>
</div>

{% endblock %}
